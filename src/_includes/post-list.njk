{% if collections.posts.length > 0 %}
{% for post in collections.posts -%}
<p>{{ post.date | shortDate }} <a href="{{ post.url }}">{{ post.data.title }}</a></p>
{%- endfor %}
{% else %}
<p>No blog posts yet. Check back soon!</p>
{% endif %}